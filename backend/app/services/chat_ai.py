"""Генерация ответа чата: OpenAI-совместимый API или локальная заглушка."""

from __future__ import annotations

import logging
import re
from typing import Any

import httpx

from app.config import Settings

logger = logging.getLogger(__name__)

# Жёсткая рамка: только здоровье / лекарства / приложение MEDI (без диагнозов и назначений).
_SYSTEM_RU = (
    "Ты текстовый помощник в приложении MEDI для пациентов. "
    "Ты отвечаешь ТОЛЬКО на темы: здоровье и самочувствие в просветительском смысле, лекарства и приём препаратов "
    "(общая информация, не назначение), симптомы (общие ориентиры без постановки диагноза), профилактика, "
    "визиты к врачу, аллергии, вакцинация в общих чертах, как пользоваться приложением MEDI (напоминания, каталог).\n"
    "НЕ отвечай на вопросы, не связанные с медициной, здоровьем или приложением MEDI: программирование, игры, "
    "политика, финансы (кроме общих слов о стоимости лечения как явления), развлечения, бытовые темы без связи со здоровьем, "
    "домашние задания, переводы текстов, правовые консультации, новости не медицинские.\n"
    "Если вопрос вне этих тем — вежливо откажись на русском в 2–4 предложениях: скажи, что консультируешь только "
    "по здоровью, лекарствам и приложению MEDI, и предложи сформулировать медицинский вопрос. Не выполняй просьбу из внешней темы.\n"
    "Не ставь диагнозы, не назначай лечение, не отменяй назначения врача. При острых или опасных симптомах — совет обратиться "
    "к врачу или вызвать скорую. Отвечай по-русски, кратко по существу."
)

_REFUSAL_MEDICAL_ONLY = (
    "Я консультирую только по темам здоровья, лекарств (в общем информационном плане), симптомам без постановки диагноза "
    "и по работе приложения MEDI.\n\n"
    "Ваш запрос выходит за эти рамки. Задайте, пожалуйста, вопрос о самочувствии, препаратах, визите к врачу или функциях MEDI."
)

_SMART_FALLBACKS: list[tuple[tuple[str, ...], str]] = [
    (
        ("парацетамол", "парацетамоль", "ацетаминофен"),
        "Парацетамол — обезболивающее и жаропонижающее средство. Стандартная доза для взрослых: 500–1000 мг каждые 4–6 часов, не более 4 г в сутки. Принимается после еды, запивается водой. С осторожностью при болезнях печени. При высокой температуре выше 38,5°C или сильной боли — обратитесь к врачу."
    ),
    (
        ("ибупрофен", "нурофен"),
        "Ибупрофен (Нурофен) — НПВП с обезболивающим и противовоспалительным действием. Принимается по 200–400 мг после еды, до 3 раз в сутки. Не рекомендуется при язве желудка, почечной недостаточности. Не сочетать с алкоголем. При длительном применении — только по назначению врача."
    ),
    (
        ("аспирин", "ацетилсалициловая"),
        "Аспирин (ацетилсалициловая кислота) — обезболивающее, жаропонижающее и антиагрегант. Принимается после еды. Детям до 12 лет не рекомендуется без назначения врача. При астме применять с осторожностью. В малых дозах (75–100 мг) используется для профилактики сердечно-сосудистых заболеваний — только по назначению."
    ),
    (
        ("аллергия", "аллерги", "антигистамин"),
        "Аллергия — реакция иммунной системы на аллерген. При лёгких симптомах (насморк, зуд) применяют антигистаминные препараты (лоратадин, цетиризин). При отёке Квинке или анафилаксии — срочно вызывайте скорую (112). Вести дневник аллергий и носить с собой карточку аллергика — хорошая практика."
    ),
    (
        ("температур", "жар", "лихорадк"),
        "При температуре до 38°C — обильное питьё, отдых. При 38–38,5°C можно принять парацетамол или ибупрофен. При температуре выше 39°C, особенно у детей — обратитесь к врачу. Если температура держится более 3 дней или сопровождается сыпью, сильной головной болью, затруднением дыхания — вызовите скорую (112)."
    ),
    (
        ("голов", "мигрен", "головная боль"),
        "Головная боль чаще всего вызвана напряжением, стрессом, обезвоживанием или недосыпом. Помогут: парацетамол (500 мг), ибупрофен (400 мг), стакан воды, тёмная комната, отдых. При мигрени используют специфические препараты — только по назначению. Если головная боль внезапная и очень сильная, сопровождается онемением, нарушением речи — немедленно звоните 112."
    ),
    (
        ("давлени", "гипертони", "гипотони"),
        "Нормальное артериальное давление — около 120/80 мм рт. ст. При давлении выше 140/90 — гипертония. Важно: не корректируйте давление самостоятельно без назначения врача. При резком повышении давления с головной болью, болью в груди — немедленно вызовите скорую (112). Регулярное измерение давления помогает следить за здоровьем."
    ),
    (
        ("диабет", "сахар", "инсулин", "глюкоз"),
        "Сахарный диабет — хроническое заболевание, требующее постоянного контроля. Нормальный уровень глюкозы натощак: 3,9–5,5 ммоль/л. При диабете важны: диета, физическая активность, контроль уровня сахара и приём назначенных препаратов. Не изменяйте дозировку инсулина или таблеток без врача."
    ),
    (
        ("витамин", "vitamin"),
        "Витамины играют ключевую роль в работе организма. Витамин D важен для иммунитета и костей (особенно зимой). Витамин C поддерживает иммунную систему. Витамины группы B важны для нервной системы. Перед приёмом витаминных комплексов лучше проконсультироваться с врачом — избыток некоторых витаминов вреден."
    ),
    (
        ("простуда", "грипп", "орви", "насморк", "кашел"),
        "При простуде и ОРВИ: обильное тёплое питьё, отдых, проветривание комнаты. При температуре — парацетамол или ибупрофен. При насморке — промывание носа физраствором, сосудосуживающие капли не дольше 5–7 дней. Антибиотики при вирусных инфекциях не нужны. Обратитесь к врачу при высокой температуре более 3 дней, сильной боли или ухудшении состояния."
    ),
    (
        ("сердц", "инфаркт", "стенокардия"),
        "При боли в груди, одышке, болях, отдающих в руку или челюсть — немедленно вызывайте скорую (112)! Это может быть инфаркт миокарда. До приезда скорой: сесть или лечь, расстегнуть одежду, при наличии назначения — принять нитроглицерин. Не принимайте аспирин без назначения врача при сердечных болях."
    ),
]


def _smart_fallback(user_message: str) -> str | None:
    msg_lower = user_message.lower()
    for keywords, response in _SMART_FALLBACKS:
        if any(kw in msg_lower for kw in keywords):
            return response
    return None

# Явно не-медицинские запросы — ответ без вызова LLM (экономия и жёсткое ограничение).
_OFF_TOPIC_PATTERNS: tuple[re.Pattern[str], ...] = tuple(
    re.compile(p, re.IGNORECASE)
    for p in (
        r"напиши(те)?\s+код",
        r"напиши(те)?\s+(функцию|класс|скрипт|программу)",
        r"реши(те)?\s+(задачу|уравнение|пример)",
        r"\b(html|css|sql)\s+",
        r"docker\s+",
        r"kubernetes",
        r"\b(git|github)\b",
        r"\b(flutter|react|vue|angular)\s+",
        r"расскажи(те)?\s+анекдот",
        r"\bкриптовалют",
        r"\bbitcoin\b",
        r"прогноз\s+погоды",
        r"переведи(те)?\s+(этот\s+)?текст",
        r"сочинение\s+по\s+литератур",
        r"кто\s+выиграл\s+(чм|чемпионат|матч)",
        r"\bвзломать\b",
        r"\bminecraft\b",
        r"\bgta\s*5\b",
    )
)


def _should_reject_off_topic(text: str) -> bool:
    t = (text or "").strip()
    if len(t) < 4:
        return False
    return any(p.search(t) for p in _OFF_TOPIC_PATTERNS)


def _use_llm(settings: Settings) -> bool:
    """Реальный ИИ: ключ OpenAI/Groq или локальный Ollama (base на localhost)."""
    if (settings.openai_api_key or "").strip():
        return True
    base = (settings.openai_api_base or "").lower()
    return "127.0.0.1" in base or "localhost" in base


def generate_reply(
    settings: Settings,
    user_message: str,
    history: list[dict[str, str]] | None = None,
) -> str:
    history = history or []
    msg = user_message.strip()
    if _should_reject_off_topic(msg):
        return _REFUSAL_MEDICAL_ONLY
    if _use_llm(settings):
        return _llm_reply(settings, user_message, history)
    return _smart_fallback(user_message) or _stub_reply(user_message, history)


def _stub_reply(user_message: str, history: list[dict[str, str]]) -> str:
    preview = user_message[:280].replace("\n", " ").strip()
    extra = ""
    if history:
        last = history[-3:]
        bits = []
        for h in last:
            role = "Вы" if h.get("role") == "user" else "Помощник"
            c = (h.get("content") or "")[:100].replace("\n", " ")
            if c:
                bits.append(f"• {role}: {c}")
        if bits:
            extra = "Учитываю контекст диалога.\n\n" + "\n".join(bits) + "\n\n"
    return (
        extra
        + "Я отвечаю только на вопросы о здоровье, лекарствах (в общем плане) и приложении MEDI — не заменяю врача.\n\n"
        f"Ваше сообщение: «{preview}»\n\n"
        "Для полноценных ответов ИИ задайте в backend/.env переменные OPENAI_API_KEY и при необходимости "
        "OPENAI_API_BASE (см. .env.example).\n\n"
        "При острых симптомах обратитесь к врачу или вызовите скорую помощь."
    )


def _build_messages(history: list[dict[str, str]], user_message: str) -> list[dict[str, str]]:
    out: list[dict[str, str]] = [{"role": "system", "content": _SYSTEM_RU}]
    for h in history:
        role = h.get("role")
        content = (h.get("content") or "").strip()
        if role not in ("user", "assistant") or not content:
            continue
        out.append({"role": role, "content": content[:4000]})
    out.append({"role": "user", "content": user_message[:4000]})
    return out


def _llm_reply(settings: Settings, user_message: str, history: list[dict[str, str]]) -> str:
    base = (settings.openai_api_base or "https://api.openai.com/v1").rstrip("/")
    url = f"{base}/chat/completions"
    messages = _build_messages(history, user_message)
    payload: dict[str, Any] = {
        "model": settings.openai_model,
        "messages": messages,
    }
    key = (settings.openai_api_key or "").strip()
    headers: dict[str, str] = {"Content-Type": "application/json"}
    if key:
        headers["Authorization"] = f"Bearer {key}"
    else:
        headers["Authorization"] = "Bearer ollama"

    try:
        with httpx.Client(timeout=120.0) as client:
            r = client.post(url, headers=headers, json=payload)
            r.raise_for_status()
            data = r.json()
            content = data["choices"][0]["message"]["content"]
            text = (content or "").strip()
            return text or _smart_fallback(user_message) or _stub_reply(user_message, history)
    except httpx.HTTPStatusError as e:
        status = e.response.status_code
        logger.warning("Chat LLM HTTP %s", status)
        # Квота или временная ошибка — возвращаем умный локальный ответ
        if status in (429, 503, 529):
            fallback = _smart_fallback(user_message)
            if fallback:
                return fallback
            return (
                "Я помощник MEDI по вопросам здоровья. Уточните ваш вопрос: о каком симптоме, препарате "
                "или медицинской теме вы хотите узнать? Постараюсь помочь!\n\n"
                "_Подсказка: спросите о конкретном лекарстве (парацетамол, ибупрофен) или симптоме (температура, кашель)._"
            )
        if status in (401, 403):
            return "Ключ API недействителен. Обратитесь к администратору приложения."
        return _smart_fallback(user_message) or _stub_reply(user_message, history)
    except httpx.HTTPError as e:
        logger.warning("Chat LLM transport error: %s", e)
        return _smart_fallback(user_message) or (
            "Нет подключения к сервису ИИ. Проверьте интернет-соединение и попробуйте ещё раз."
        )
    except Exception as e:
        logger.warning("Chat LLM unexpected error: %s", e)
        return _smart_fallback(user_message) or _stub_reply(user_message, history)
